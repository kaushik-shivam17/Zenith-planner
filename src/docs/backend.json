{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Zenith Planner application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user (matches Firebase Auth UID)."
        },
        "name": {
          "type": "string",
          "description": "The user's full name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "class": {
          "type": "string",
          "description": "The user's class or grade (optional)."
        },
        "height": {
          "type": "number",
          "description": "User's height in meters (optional)."
        },
        "weight": {
          "type": "number",
          "description": "User's weight in kilograms (optional)."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "createdAt",
        "updatedAt"
      ]
    },
    "Roadmap": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Roadmap",
      "type": "object",
      "description": "Represents an AI-generated roadmap for a task or mission.",
      "properties": {
        "introduction": {
          "type": "string"
        },
        "milestones": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "title": { "type": "string" },
              "emoji": { "type": "string" },
              "steps": { "type": "array", "items": { "type": "string" } }
            },
            "required": ["title", "emoji", "steps"]
          }
        },
        "conclusion": {
          "type": "string"
        }
      },
      "required": ["introduction", "milestones", "conclusion"]
    },
    "Task": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Task",
      "type": "object",
      "description": "Represents a single task for a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the task."
        },
        "userId": {
          "type": "string",
          "description": "The Firebase Auth UID of the user who owns this task."
        },
        "title": {
          "type": "string",
          "description": "The title of the task."
        },
        "description": {
          "type": "string",
          "description": "Optional details about the task."
        },
        "deadline": {
          "type": "string",
          "format": "date-time",
          "description": "The deadline for the task as a Firestore Timestamp."
        },
        "completed": {
          "type": "boolean",
          "description": "Whether the task has been completed."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the task was created."
        },
        "roadmap": {
          "$ref": "#/backend/entities/Roadmap",
          "description": "The AI-generated roadmap for this task."
        }
      },
      "required": [
        "id",
        "userId",
        "title",
        "deadline",
        "completed",
        "createdAt"
      ]
    },
    "Mission": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Mission",
      "type": "object",
      "description": "Represents a long-term goal or mission for a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the mission."
        },
        "userId": {
          "type": "string",
          "description": "The Firebase Auth UID of the user who owns this mission."
        },
        "title": {
          "type": "string",
          "description": "The title of the mission."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the mission was created."
        },
        "totalGoals": {
          "type": "number",
          "description": "The total number of goals for this mission."
        },
        "completedGoals": {
          "type": "number",
          "description": "The number of completed goals for this mission."
        },
        "roadmap": {
          "$ref": "#/backend/entities/Roadmap",
          "description": "The AI-generated roadmap for this mission."
        }
      },
      "required": [
        "id",
        "userId",
        "title",
        "createdAt",
        "totalGoals",
        "completedGoals"
      ]
    },
    "Goal": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Goal",
      "type": "object",
      "description": "Represents a single goal or sub-task for a mission.",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the goal."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who owns this goal."
        },
        "missionId": {
          "type": "string",
          "description": "The ID of the mission this goal belongs to."
        },
        "title": {
          "type": "string",
          "description": "The title of the goal."
        },
        "description": {
          "type": "string",
          "description": "Optional details about the goal."
        },
        "completed": {
          "type": "boolean",
          "description": "Whether the goal has been completed."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the goal was created."
        }
      },
      "required": [
        "id",
        "userId",
        "missionId",
        "title",
        "completed",
        "createdAt"
      ]
    },
    "TimetableEvent": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Timetable Event",
      "type": "object",
      "description": "Represents a single event on the user's timetable.",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique identifier for the event."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who owns this event."
        },
        "title": {
          "type": "string",
          "description": "The title of the event."
        },
        "day": {
          "type": "string",
          "description": "The day of the week for the event."
        },
        "startTime": {
          "type": "string",
          "description": "The start time of the event."
        },
        "endTime": {
          "type": "string",
          "description": "The end time of the event."
        },
        "type": {
          "type": "string",
          "description": "The type of event ('custom' or 'task')."
        }
      },
      "required": [
        "id",
        "userId",
        "title",
        "day",
        "startTime",
        "endTime",
        "type"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information, including BMI-related data. Users can only read/write their own document.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user. This is the Firebase Auth UID."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/tasks/{taskId}",
        "definition": {
          "entityName": "Task",
          "schema": {
            "$ref": "#/backend/entities/Task"
          },
          "description": "Stores tasks for a specific user. Users can only read/write their own tasks.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user (Firebase Auth UID)."
            },
            {
              "name": "taskId",
              "description": "The unique identifier for the task."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/missions/{missionId}",
        "definition": {
          "entityName": "Mission",
          "schema": {
            "$ref": "#/backend/entities/Mission"
          },
          "description": "Stores long-term missions for a specific user. Users can only read/write their own missions.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user (Firebase Auth UID)."
            },
            {
              "name": "missionId",
              "description": "The unique identifier for the mission."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/missions/{missionId}/goals/{goalId}",
        "definition": {
          "entityName": "Goal",
          "schema": {
            "$ref": "#/backend/entities/Goal"
          },
          "description": "Stores goals for a specific mission. Users can only read/write their own goals.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            },
            {
              "name": "missionId",
              "description": "The unique identifier for the mission."
            },
            {
              "name": "goalId",
              "description": "The unique identifier for the goal."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/timetableEvents/{eventId}",
        "definition": {
          "entityName": "TimetableEvent",
          "schema": {
            "$ref": "#/backend/entities/TimetableEvent"
          },
          "description": "Stores timetable events for a specific user. Users can only read/write their own events.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            },
            {
              "name": "eventId",
              "description": "The unique identifier for the timetable event."
            }
          ]
        }
      }
    ]
  }
}
